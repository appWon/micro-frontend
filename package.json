{
  "name": "micro-front",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "yarn workspaces foreach run dev",
    "build": "yarn workspaces foreach run build",
    "build:changed": "yarn workspaces foreach --since=origin/main run build "
  },
  "devDependencies": {
    "typescript": "^4.7.4"
  }
}
